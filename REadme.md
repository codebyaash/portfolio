# Aashritha N — Portfolio
Recruiter-focused static portfolio. No build step required.

Before publishing, replace `YOUR_EMAIL`, `YOUR_LINKEDIN_URL`, and `YOUR_GITHUB_URL` in `index.html`; add exact dates, achievements, credentials, education, project links, and a root-level `resume.pdf`.

Preview with `python3 -m http.server 8000`. Deploy directly to GitHub Pages, Netlify, or Vercel.
